<!-- views/user/product-detail.hbs -->
<style>
  .product-image {
    width: 100%;
    height: auto;
    max-height: 500px; /* Adjust as needed */
    object-fit: cover; /* Maintain aspect ratio and cover the area */
  }
</style>

<section>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-6">
        <img class="product-image" src="/product-images/{{product._id}}.jpg" alt="Product Image">
      </div>
      <div class="col-md-6">
        <h2>{{product.Name}}</h2>
        <p>Category: {{product.Category}}</p>
        <p>Price: Rs.{{product.Price}}</p>
        <p>{{product.Description}}</p>
        <button onclick="addToCart('{{product._id}}')" class="btn btn-primary">Add To Cart</button>
        <button onclick="addToWishlist('{{product._id}}')" class="btn btn-warning">Add To Wishlist</button>
      </div>
    </div>
  </div>
</section>

<script>


function addToWishlist(productId) {
  // Add to wishlist functionality
  alert("product added to wishlist")
}
</script>
